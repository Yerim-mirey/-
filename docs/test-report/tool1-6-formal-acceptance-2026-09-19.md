# Tool 1–6 正式端到端验收记录（2026-09-19）

## 结论

**未通过，待百度步行路由配额/限流问题解除后重验。** 这次运行使用同一 BD09LL 坐标地点、三类设施和服务默认业务参数；没有使用之前单类别、缩小采样、8 对路由预算的 smoke 配置。诊断 Envelope 为 `ok=true`，但这只表示可返回部分结果，**不等于正式验收通过**。

## 验收配置与保护

- Tool 4 默认 24 方向采样；Tool 5 默认 200m 网格、1000m 步行距离、最多 5000 路由目标对；Tool 6 请求全部三类设施。
- 使用本机环境变量中的 AK，未保存或输出 AK、原始百度响应、请求 URL 或 POI 身份信息。
- 仅执行一次。设置路由 HTTP 300 次、POI HTTP 60 次安全上限；实际远未触及上限。路由 Provider 返回其被代码识别为 `RATE_LIMITED` 的状态后，熔断后续真实路由 HTTP 调用，没有反复重试整套验收。
- 本次 Location 使用坐标输入；地址地理编码分支未在这次运行中重新验收。

## 观察结果

| 检查项 | 结果 |
| --- | --- |
| POI 检索 | 完成；市场 35、药店 44、小学 22；共 18 次 POI HTTP 请求 |
| 等时圈 | 24/24 个边界方向有效；192 个路由样本中 144 个成功，置信度 `medium`，阶段 `partial` |
| POI 搜索半径 | 2129m，等于当前等时圈按 `R + 1000m + 100m` 算出的最低半径 |
| 中心到设施 Routing | 百度步行路由在第 4 次 HTTP 请求触发项目识别的限流状态；阶段 `unavailable`，15 分钟确认可达数均为 `null` |
| Blindspot | 54 个网格；所有三类的 `unknown_ratio=1.0`，`blind_ratio=0`，阶段 `partial`；未把缺失路由误判为盲区 |
| 几何与接口 | 等时圈和输出 Feature 拓扑有效，盲区/未知区 Feature 均在分析域内，Tool 6 成对接口校验通过 |
| 总体 | `confidence=low`，`formal_acceptance_passed=false` |

Tool 5 内部记录的 1380 个目标对在熔断后均为不可用；这不是 1380 次百度 HTTP 请求。真实路由 HTTP 请求共 4 次。

## 后续

先在百度开放平台核对该 AK 的步行路线配额/限流状态；恢复后只安排一次同配置重验。验收通过条件是：Location、POI、Routing、Isochrone、Blindspot 阶段均完整，三类 `counts` 为确定整数，半径达标，几何有效且位于分析域内，三类 `unknown_cells=0`。若仍出现部分结果，继续记录原因，不把 `ok=true` 当成通过。
