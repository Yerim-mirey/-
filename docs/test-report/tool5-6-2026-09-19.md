# Tool 5/6 验证记录（2026-09-19）

## 离线

- `python scripts/validate_contracts.py`：Tool 1–6 v1 示例与成对接口校验通过。
- `python -m pytest -q -p no:cacheprovider`：`184 passed, 3 skipped`；跳过的三项是已有 Tool 的 opt-in 百度联网测试。
- Tool 5 测试涵盖网格裁剪、真实等时圈保存样本、`counts=0/null`、半径不足、预算耗尽、路由失败和几何无效。
- Tool 6 测试用完整 Mock 链路核对正常、部分结果与各上游失败的降级语义。

## 受控真实百度链路

在本机 AK 环境下显式启动 `scripts/verify_diagnosis_live.py`，使用单一设施类别 `primary_school`、8 个方向的缩小版等时圈采样、Tool 5 最多 8 个网格到设施路由对。没有输出 AK、原始响应或 POI 标识。

- Diagnosis Envelope：`ok=true`。
- POI 检索半径：2129m，覆盖等时圈最远点外 1km 及 100m 余量。
- 小学 POI：22 条；中心 15 分钟步行确认可达 2 条。
- 各阶段：Location、POI、Routing、Isochrone `complete`；Blindspot `partial`。
- Blindspot `blind_ratio=0`、`unknown_ratio≈0.99937`，总体置信度 `low`。原因是刻意限制路由预算，未测网格保持 `unknown`，不被误判为 `blind`。

这证明真实 Provider 能走通 Tool 1–6 的接口，但不是完整范围盲区结果，也不是地图可视化验收。正式业务结果应使用默认采样和预算，并控制百度配额；遇到部分请求失败或预算耗尽时继续保留 `unknown`。
